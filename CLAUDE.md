# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Ally OS is a project in early development. The repository is organized as a monorepo-style structure with the following directories:

- `libraries/` - Shared libraries and packages
- `templates/` - Project templates and boilerplate
- `workflows/` - Automation workflows and task definitions

## Development Setup

This is a Python-based project (indicated by `.gitignore` patterns). Build, test, and lint commands will be documented here as the project matures.

## Architecture

The project structure suggests a modular architecture where:
- Reusable code lives in `libraries/`
- Starter templates live in `templates/`
- Automated processes live in `workflows/`

Configuration files and tooling have not yet been established.
